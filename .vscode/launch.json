{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run integration_endpoint_check.py",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/integration_endpoint_check.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}
